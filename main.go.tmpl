package main

import (
	"bufio"
	_ "embed"
	"fmt"

	//    "regexp"
	//    "strconv"
	"strings"
)

//go:embed input.txt
var input string

func parseLine(line string) string {
	return line
}

func parseLines(i string) []int {
	res := []int{}
	scanner := bufio.NewScanner(strings.NewReader(i))

	for scanner.Scan() {
		line := scanner.Text()
		_ = parseLine(line)
	}

	return res
}

func processLines(lines []int) int {
	var result int

	return result
}

func run(i string) int {
	lines := parseLines(i)

	answer := processLines(lines)

	return answer
}

func main() {
	answer := run(input)
	fmt.Println("Answer: ", answer)
}
